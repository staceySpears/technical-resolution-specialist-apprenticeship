# Service Management

> **STATUS: Stub — complete during Week 1, Day 5.**

To cover:
- `systemctl status / start / stop / restart / enable / disable`
- `systemctl --failed`
- `journalctl -u <service>` and `journalctl -p 3`
- Enabled vs. active — and why both matter in production
- Runbook: investigating a failed service after a patch
