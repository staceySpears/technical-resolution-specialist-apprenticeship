# Process Management

> **STATUS: Stub — complete during Week 1, Day 3.**

To cover:
- `ps aux`, sorting by CPU and memory
- `top`, `uptime`, `free -h`
- Safely killing a runaway process (`kill`, when to use `kill -9`)
- Production scenario: high CPU causing user-facing slowness
- Support procedure: confirm alert → identify process → capture evidence → escalate → document
